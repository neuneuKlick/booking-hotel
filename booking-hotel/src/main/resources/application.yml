spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/booking_db
    username: postgres
    password: postgres

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.xml